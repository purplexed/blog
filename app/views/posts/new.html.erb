<%= form_for(@post) do |f| %>
	
  	<%= f.label :title %><br/>
  	<%= f.text_field :title %>

  	<% f.label :body %><br />
  	<%= f.text_field :body %>

  <div class="actions">
  	<%= f.submit%>
  </div>
<% end %>