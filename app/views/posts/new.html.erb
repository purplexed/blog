<%= form_for(@post) do |f| %>
	<% if @post.errors.any? %>
		<%= @post.errors.full_messages %><br />
	<% end %>
  	<%= f.label :title %><br/>
  	<%= f.text_field :title %>

  	<%= f.label :body %><br />
  	<%= f.text_area :body %>

  <div class="actions">
  	<%= f.submit %>
  </div>

<% end %>